import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'iw.p1u4.saga',
  appName: 'iw-p1u4-saga',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
